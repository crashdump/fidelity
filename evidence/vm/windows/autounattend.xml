<?xml version="1.0" encoding="utf-8"?>
<!-- The answer file for the specialize and oobe passes. `install.cmd` copies
     it to `C:\Windows\Panther\unattend.xml`, the standard offline location,
     and Windows reads it on the first boot after the image is applied.

     It has no windowsPE pass, because `install.cmd` does that pass now: it
     partitions the disk, applies the image, and injects the driver. This file
     does only what runs inside the installed system:

       1. The oobeSystem pass creates the build account and skips every
          first-boot page, which a guest cannot click through.
       2. The one FirstLogonCommand runs `setup.ps1`, which `install.cmd`
          placed on the system drive. That script installs the OpenSSH server
          and the harness key, which is how the build reaches the guest. -->
<unattend xmlns="urn:schemas-microsoft-com:unattend">
  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="arm64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <UserAccounts>
        <LocalAccounts>
          <LocalAccount wcm:action="add">
            <Name>fidelity</Name>
            <Group>Administrators</Group>
            <Password><Value>fidelity</Value><PlainText>true</PlainText></Password>
          </LocalAccount>
        </LocalAccounts>
      </UserAccounts>
      <AutoLogon>
        <Enabled>true</Enabled>
        <Username>fidelity</Username>
        <LogonCount>3</LogonCount>
        <Password><Value>fidelity</Value><PlainText>true</PlainText></Password>
      </AutoLogon>
      <OOBE>
        <HideEULAPage>true</HideEULAPage>
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <HideWirelessSetupInOOBE>true</HideWirelessSetupInOOBE>
        <ProtectYourPC>3</ProtectYourPC>
        <SkipMachineOOBE>true</SkipMachineOOBE>
        <SkipUserOOBE>true</SkipUserOOBE>
      </OOBE>
      <FirstLogonCommands>
        <SynchronousCommand wcm:action="add">
          <Order>1</Order>
          <CommandLine>powershell -NoProfile -ExecutionPolicy Bypass -File C:\setup.ps1</CommandLine>
        </SynchronousCommand>
      </FirstLogonCommands>
    </component>
  </settings>
</unattend>
