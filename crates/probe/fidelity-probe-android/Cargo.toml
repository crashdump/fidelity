[package]
name = "fidelity-probe-android"
description = "Android operating-system access for the fidelity runtime tamper detection library."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[target.'cfg(target_os = "android")'.dependencies]
fidelity-cipher = { path = "../../fidelity-cipher", version = "0.0.0" }
fidelity-core = { path = "../../fidelity-core", version = "0.0.0" }
fidelity-formats = { path = "../../fidelity-formats", version = "0.0.0" }
fidelity-types = { path = "../../fidelity-types", version = "0.0.0" }

[lints]
workspace = true
